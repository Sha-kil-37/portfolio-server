meta {
  name: reset code verify
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/admin/reset-code-verify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "code":"962865"
  }
}
