meta {
  name: reset password
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/admin/reset-pass
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "newPassword":"123456"
  }
}
